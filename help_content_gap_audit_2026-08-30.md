# ヘルプ内容と現行アプリの差異調査（2026-08-30）

## 調査範囲

- ヘルプ一覧: `Shuffle100/SwiftUI Views/viewModels/HelpList.ViewModel.swift`
- ヘルプ本文: `Shuffle100/resources/html/*.html`
- 現行UI・動作: `HomeView`、`ReciteSettingsView`、`PoemPickerView`、各 `GameStrategy`、`GameStateManager`

## 明確な食い違い

### 1. 「設定できること」のモード一覧が3種類しかない

`options.html` は「通常」「初心者」「ノンストップ」の3種類のみを案内している。
現行アプリでは次の5種類を選べる。

1. 通常（競技かるた）
2. 初心者（チラし取り）
3. ノンストップ（止まらない）
4. ノンストップ（下の句のみ）
5. 下の句かるた（北海道式）

不足: 「ノンストップ（下の句のみ）」「下の句かるた（北海道式）」。

根拠:

- ヘルプ: `Shuffle100/resources/html/options.html:23-30`
- 現行実装: `Shuffle100/SwiftUI Views/Home/HomeView.swift:220-225`

### 2. 序歌は常に「下の句を2回」と読める説明になっている

通常モード・初心者モードの流れでは、序歌の下の句は2回読まれると断定している。
現行アプリには「序歌の読み上げ時間を短縮」設定があり、有効時は下の句を1回だけ読む。
また「下の句かるた」「ノンストップ（下の句のみ）」は設定にかかわらず短縮版になる。

根拠:

- ヘルプ: `game_flow.html:22-25`、`beginner_mode_flow.html:20-23`
- 設定UI: `ReciteSettingsView.swift:97-103`
- 適用処理: `GameStateManager.swift:108-112`
- 強制短縮: `HokkaidoGameStrategy.swift`、`NonstopShimoGameStrategy.swift`

### 3. 感想戦の設定画像が現行画面と異なる

本文の操作名は概ね正しいが、`enable_postmortem.png` は旧UIで、右上に「設定終了」がある。
現行画面はセクション分けされたSwiftUI画面で、右上は×の閉じるボタン。また現行画面には「序歌の読み上げ時間を短縮」も並ぶ。

根拠:

- 旧画像: `Shuffle100/resources/html/enable_postmortem.png`
- 現行実装: `Shuffle100/SwiftUI Views/Settings/ReciteSettingsView.swift:26-50,95-113`

### 4. 暗記時間タイマーの画像が現行トップ画面と異なる

`memorizeTimerCell.jpg` は旧UIのトップ画面（各行が開く形式、旧レイアウト）を示している。
現行トップ画面は、モードと読手がダイアログ選択、暗記時間タイマーが「試合開始」セクション内のボタンになっている。
本文の「通常モードのみ表示」という説明自体は現行実装と一致する。

根拠:

- 旧画像: `Shuffle100/resources/html/memorizeTimerCell.jpg`
- 現行実装: `Shuffle100/SwiftUI Views/Home/HomeView.swift:40-125`

## 現行機能の説明漏れ（内容不足）

### 5. 「設定できること」が現行設定の全体を説明していない

本文で案内しているのは、読手、モード、歌の選択、空札のみ。
現行アプリで設定できる次の項目が抜けている。

- 歌と歌の間隔
- 上の句と下の句の間隔
- 音量調整
- 序歌の読み上げ時間を短縮
- 試合後に感想戦を選択できる
- 暗記時間タイマー（通常モードのみ。別ページには説明あり）

根拠:

- ヘルプ: `Shuffle100/resources/html/options.html`
- 現行実装: `ReciteSettingsView.swift:28-37,63-113`、`HomeView.swift:103-125`

### 6. 「まとめて選ぶ」で使える現行の選択方法がほぼ説明されていない

ヘルプは札セットと五色百人一首だけを個別に説明しているが、現行メニューには次もある。

- 1字目で選ぶ
- 1の位の数で選ぶ
- 10の位の数で選ぶ

根拠: `Shuffle100/SwiftUI Views/PoemPicker/PoemPickerToolbarButtons.swift:57-93`

### 7. 「ノンストップ（下の句のみ）」の専用説明がない

現行アプリには、下の句だけを最後まで自動で連続再生する独立モードがある。
ヘルプ一覧には通常のノンストップと下の句かるたの説明はあるが、このモードの説明項目がない。

根拠:

- ヘルプ一覧: `Shuffle100/SwiftUI Views/viewModels/HelpList.ViewModel.swift:22-32`
- 現行モード: `HomeView.swift:220-225`、`NonstopShimoGameStrategy.swift`

### 8. ノンストップの説明が「上の句・下の句を連続」だと明示していない

`what_is_nonstop_mode.html` は「最後の歌まで自動で読み続ける」とだけ書いている。
下の句のみのノンストップが追加された現状では、既存ノンストップが「上の句→下の句」を連続再生するモードであることを明記しないと区別しづらい。

## 画像・表現の陳腐化

### 9. 札セット・五色百人一首の説明画像が旧UI

以下の画像は旧UIKit画面の外観で、現行SwiftUI画面と見た目が異なる。
操作名と操作経路（「保存」「まとめて選ぶ」）は現行でも概ね一致している。

- `save_set_button.png`
- `select_by_group.png`
- `select_by_fuda_set.png`
- `delete_fuda_set.png`
- `select_by_5_colors.png`

### 10. 通常・初心者モードの説明動画が現在のUIを示していない可能性が高い

ヘルプは2014年公開時から使われているVimeo動画へ誘導している。現在はSwiftUI NavigationStackベースへ移行済みのため、少なくとも画面操作の見た目は現行と一致しない可能性が高い。
動画内容そのものは今回オフラインのコード調査では未検証。

- 通常: `https://vimeo.com/88511077`
- 初心者: `https://vimeo.com/104796183`

## 現時点で実装と一致している主な説明

- 初心者モードは上の句から下の句へ自動進行し、その後「次はどうする？」を表示する。
- 下の句かるたは短縮序歌の後、下の句だけを読み、各首後に「次はどうする？」を表示する。
- 下の句かるたで「次の歌へ」を選ぶと、直前の下の句を再読してから次の下の句へ進む。
- 空札は初心者モードでは設定できず、選択数が50首未満なら同数を追加、50首以上なら全100首になる。
- 暗記時間タイマーは通常モードだけに表示される。
- 感想戦は同じ歌を同じ順番で再生する。

## 修正優先度案

1. `options.html` の5モード対応と現行設定項目の追記（2026-08-30 対応済み）
2. 「ノンストップ（下の句のみ）」の専用ヘルプ追加（2026-08-30 対応済み）
3. 序歌2回の断定を短縮設定込みの説明に修正（2026-08-30 対応済み）
4. 旧UI画像の全差し替え（2026-08-30 対応済み）
5. 古い動画リンクの再撮影または削除判断
6. 「まとめて選ぶ」の未説明3機能を追記
